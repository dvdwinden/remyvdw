<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "Remy van der Winden" }}</title>
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,100;1,200;1,300;1,400;1,500;1,600;1,700&display=swap" rel="stylesheet">

  <link rel="icon" type="image/png" href="{{ '/favicon.png' | url }}">
  
  {# Social sharing meta tags #}
  <meta property="og:title" content="{{ title or 'Remy van der Winden: Motion Designer' }}">
  <meta property="og:description" content="Motion Designer from the Netherlands with over twelve years of design experience. Available for freelance projects.">
  <meta property="og:image" content="{{ '/assets/share-image.jpeg' | url | absoluteUrl(metadata.url) }}">
  <meta property="og:type" content="website">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{{ title or 'Remy van der Winden: Motion Designer' }}">
  <meta name="twitter:description" content="Motion Designer from the Netherlands with over twelve years of design experience. Available for freelance projects.">
  <meta name="twitter:image" content="{{ '/assets/share-image.jpeg' | url | absoluteUrl(metadata.url) }}">
  
  {# Preload large GIF for better performance #}
  <link rel="preload" as="image" href="{{ '/assets/carbo-culture.gif' | url }}">
  <link rel="preload" as="image" href="{{ '/assets/liquid-gold.gif' | url }}">
  
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
</head>
<body class="bg-[#eee] dark:bg-[#222] text-gray-900 dark:text-white">
  <main class="prose dark:prose-invert px-6 md:px-4" style="margin-top: 10em;">
    {{ content | safe }}
  </main>
</body>
</html>
